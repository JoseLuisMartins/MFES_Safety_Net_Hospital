class Person
instance variables
  protected name: ModelUtils`String := [];
  protected age: nat;
  
operations

	 protected retName: () ==> ModelUtils`String
	 retName() == (
	 		return name; 	
	 );
	
	 protected retAge: () ==> nat
	 retAge() == (
	 		return age; 	
	 );
	 

end Person