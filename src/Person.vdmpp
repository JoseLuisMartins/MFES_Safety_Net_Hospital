class Person
instance variables
  protected name: Utils`String := [];
  protected age: nat;
  
operations

	 protected retName: () ==> Utils`String
	 retName() == (
	 		return name; 	
	 );
	
	 protected retAge: () ==> nat
	 retAge() == (
	 		return age; 	
	 );
	 

end Person